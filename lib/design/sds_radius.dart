/// SDS 라운드(Border Radius) 토큰
class SdsRadius {
  SdsRadius._();

  /// 4px — 미세 라운딩
  static const double xs = 4;

  /// 8px — 버튼, 인풋
  static const double sm = 8;

  /// 12px — 카드 내부 요소
  static const double md = 12;

  /// 14px — 카드
  static const double lg = 14;

  /// 16px — 바텀시트 상단
  static const double xl = 16;

  /// 20px — 필터 칩, 탭 pill
  static const double full = 20;
}
